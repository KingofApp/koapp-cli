{
  "name": "koapp-cli",
  "version": "0.0.3",
  "description": "King of App Command Line Tools",
  "bin": {
    "koapp": "bin/koapp-cli.js"
  },
  "main": "./lib/index.js",
  "scripts": {
    "test": "test",
    "postinstall": "npm i -g cordova && npm i -g yo && npm i -g generator-koapp-module && npm i -g generator-koapp-theme && npm i -g generator-koapp-spinner"
    },
  "author": "King of App",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^2.0.0",
    "koapp": "0.0.5",
    "shelljs": "^0.7.5"
  }
}
